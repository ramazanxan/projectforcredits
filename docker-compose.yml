services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_USE_MOCK_API: "false"
        VITE_API_BASE_URL: ""
    ports:
      - "4173:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
